const ProjectCard = ({}) => {
  return (
    <section>
      <section
        id="card"
        className="card shadow-lg"
        style={{
          height: "300px",
          borderRadius: "25px",
          border: "none",
        }}
      ></section>
    </section>
  );
};

export default ProjectCard;
